services:
  # Backend API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: alsm-backend
    restart: unless-stopped
    ports:
      - "3333:3333"
    environment:
      NODE_ENV: production
      PORT: 3333
      DB_HOST: ${DB_HOST:-postgres}
      DB_PORT: ${DB_PORT:-5432}
      DB_NAME: ${DB_NAME:-alsm_ui}
      DB_USER: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-postgres}
      AUTHENTIK_URL: ${AUTHENTIK_URL}
      AUTHENTIK_TOKEN: ${AUTHENTIK_TOKEN}
      LDAP_HOST: ${LDAP_HOST:-172.17.0.1}
      LDAP_PORT: ${LDAP_PORT:-389}
      LDAP_BIND_DN: ${LDAP_BIND_DN}
      LDAP_BIND_PASSWORD: ${LDAP_BIND_PASSWORD}
      LDAP_BASE_DN: ${LDAP_BASE_DN}
      CORS_ORIGIN: http://localhost:3331
      SMTP_HOST: ${SMTP_HOST:-localhost}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_SECURE: ${SMTP_SECURE:-false}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM_NAME: ${SMTP_FROM_NAME:-ALSM}
      SMTP_FROM_ADDRESS: ${SMTP_FROM_ADDRESS:-}
      MAILSERVER_ENABLED: ${MAILSERVER_ENABLED:-true}
      MAILSERVER_DOMAIN: ${MAILSERVER_DOMAIN:-spectres.co.za}
    volumes:
      - ./backend:/app
    networks:
      - alsm-network

  # Frontend UI
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: /api
        VITE_WS_URL: /socket.io
    container_name: alsm-frontend
    restart: unless-stopped
    ports:
      - "3331:3331"
    depends_on:
      - backend
    networks:
      - alsm-network

networks:
  alsm-network:
    driver: bridge
